---
import CodeBlock from "./CodeBlock.astro";
import { GITHUB_URL } from "../consts";

const passos = `# 1. clonar e instalar
git clone ${GITHUB_URL}.git
cd revisor-notas-mcp
uv sync
cp .env.example .env

# 2. o endpoint do LLM PRECISA ser localhost (outro host é recusado)
uv run revisor-cli llm                        # confirma o LLM local

# 3. validar uma nota
uv run revisor-cli validar nota.txt           # ou cole a nota no stdin
uv run revisor-cli validar nota.txt --sem-llm # só as regras determinísticas
uv run revisor-cli anotar nota.txt            # nota com anotações inline

# 4. registrar o servidor MCP (stdio) no seu cliente, ex.: Claude Code
claude mcp add revisor-notas --scope user \\
  -e QWEN_ENDPOINT=http://127.0.0.1:8080/v1 \\
  -e QWEN_MODEL=local-model \\
  -- uv --directory /caminho/para/revisor-notas-mcp run revisor-notas-mcp`;
---

<section id="como-rodar" class="border-b border-neutral-900">
  <div class="mx-auto max-w-4xl px-6 py-20">
    <p class="text-sm font-semibold uppercase tracking-wider text-emerald">Como rodar</p>
    <h2 class="mt-3 text-2xl font-bold text-white sm:text-3xl">Roda na sua máquina</h2>
    <p class="mt-3 max-w-2xl text-neutral-400">
      Não é um serviço hospedado e não tem banco de dados. A distribuição é por
      clone + <span class="font-mono text-neutral-200">uv</span> (não há pacote no
      PyPI): você clona, aponta para o seu LLM local (obrigatoriamente localhost)
      e o servidor MCP roda ali, via stdio. Precisa de Python 3.12+ e usa o SDK
      MCP 2.x (<span class="font-mono text-neutral-200">mcp&gt;=2.2,&lt;3</span>).
      Sem LLM, a validação por regras responde sozinha.
    </p>

    <div class="mt-8">
      <CodeBlock code={passos} lang="bash" caption="Confira variáveis e opções no README do projeto." />
    </div>

    <div class="mt-6 flex gap-3 rounded-xl border border-emerald/25 bg-emerald/5 p-4">
      <svg class="mt-0.5 h-5 w-5 flex-shrink-0 text-emerald" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" d="M12 3l7 4v5c0 4.5-3 7.5-7 9-4-1.5-7-4.5-7-9V7l7-4z" />
        <path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4" />
      </svg>
      <p class="text-sm leading-relaxed text-neutral-300">
        O que sai da sua máquina: nada. O texto da nota vai só para o seu LLM
        local, pelo localhost. Um endpoint apontando para fora estoura com exceção
        em vez de degradar em silêncio, e há teste conferindo que nenhum trecho da
        nota aparece em log, mesmo quando a chamada ao LLM falha.
      </p>
    </div>
  </div>
</section>
